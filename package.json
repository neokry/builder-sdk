{
  "name": "builder-sdk",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate:sdk": "yarn workspace builder-sdk generate",
    "build:sdk": "yarn workspace builder-sdk build"
  },
  "workspaces": [
    "./packages/*",
    "./examples/*"
  ]
}
